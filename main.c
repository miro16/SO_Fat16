#include <stdio.h>
#include "file_reader.h"
#include "tested_declarations.h"
#include "rdebug.h"

int main() {
//    struct disk_t* disk = disk_open_from_file("fat16volume.img");
//
//    struct volume_t* volume = fat_open(disk, 0);
//
//    struct file_t* file = file_open(volume, "CHIEF.BIN");

    return 0;
}

